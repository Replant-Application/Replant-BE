# ===========================================
# Replant Backend 환경 설정 예시
# 이 파일을 복사하여 .env 파일을 생성하세요
# cp .env.example .env
# ===========================================

# 서버 설정
SERVER_PORT=8080
SERVER_TIMEZONE=Asia/Seoul

# AWS S3 설정
S3_BUCKET=your-bucket-name
S3_ACCESS_KEY=your-access-key
S3_SECRET_KEY=your-secret-key
S3_CLIENT=your-s3-client
S3_REGION=ap-northeast-2

# JWT 설정 (Base64 인코딩된 시크릿 키)
JWT=your-jwt-secret-key-base64-encoded

# Database 설정 (MariaDB/MySQL)
DB1_URL=jdbc:mysql://localhost:3306/replant?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
DB1_USERNAME=your-db-username
DB1_PASSWORD=your-db-password
DB_DRIVER=com.mysql.cj.jdbc.Driver
DB_CONNECTION_TIMEOUT=30000
DB_MAX_POOL_SIZE=10
DB_MIN_IDLE=5
DB_MAX_LIFETIME=1800000
DB_IDLE_TIMEOUT=600000
DB_LEAK_DETECTION_THRESHOLD=60000
DB_POOL_NAME=MariaDB-HikariPool

# Redis 설정
REDIS_URL=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password
REDIS_TIMEOUT=2000ms
REDIS_CONNECT_TIMEOUT=2000ms

# 구글 메일 설정 (메일 인증용)
GOOGLE_MAIL=your-email@gmail.com
GOOGLE_APP_PASSWORD="your-app-password"
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_SMTP_AUTH=true
MAIL_SMTP_STARTTLS_ENABLE=true
MAIL_SMTP_STARTTLS_REQUIRED=true

# 프론트엔드 URL (CORS)
FRONTEND_URL=http://localhost:8081

# 파일 업로드 설정
MAX_FILE_SIZE=10MB
MAX_REQUEST_SIZE=10MB

# Firebase Cloud Messaging (FCM) 설정
# Firebase Console에서 다운로드한 서비스 계정 키 파일 경로 (Android 앱과 동일 프로젝트 권장)
# 예: firebase/replant-admin.json (프로젝트 root의 firebase 폴더)
# 예: firebase-service-account.json (프로젝트 root)
FIREBASE_CONFIG_PATH=firebase/replant-admin.json

# ===========================================
# LLM (Google Gemini, Qwen/vLLM)
# ===========================================
# Google Gemini (Primary)
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-3-flash-preview
GEMINI_TEMPERATURE=0.7

# Qwen (vLLM) - Fallback
QWEN_API_URL=http://your-vllm-host:13127
QWEN_API_KEY=dummy
QWEN_MODEL=Qwen/Qwen2.5-14B-Instruct-AWQ
QWEN_TEMPERATURE=0.7

# Chat/LLM 공통
LLM_TIMEOUT=30000
LLM_MAX_RETRIES=2

# ===========================================
# 앱 버전 설정 (선택 업데이트 API용)
# POST /api/v1/version/check 에서 사용
# ===========================================
# APP_MIN_VERSION: 강제 업데이트용 최소 버전. 선택 업데이트만 쓸 때는 현재 앱 버전 이하로 두거나 비우기 (기본값 0.0.40)
# APP_LATEST_VERSION: 권장 업데이트용 최신 버전. 새 버전 배포 시 여기를 올리면 API로 선택 업데이트 유도 (기본값 1.0.16)
# APP_MIN_VERSION=0.0.40
# APP_LATEST_VERSION=1.0.16
# APP_STORE_URL=https://play.google.com/store/apps/details?id=com.anonymous.replantmobileapp

# Swagger/API 문서 설정
SWAGGER_UI_PATH=/swagger-ui.html
API_DOCS_PATH=/v3/api-docs

# Actuator 설정
ACTUATOR_ENDPOINTS=health,info,metrics
ACTUATOR_BASE_PATH=/actuator
ACTUATOR_HEALTH_SHOW_DETAILS=when-authorized
ACTUATOR_HEALTH_ROLES=ADMIN

# OAuth 설정 (선택사항)
# Kakao
# KAKAO_CLIENT_ID=your_kakao_client_id
# KAKAO_CLIENT_SECRET=your_kakao_client_secret

# Google
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret

# Naver
# NAVER_CLIENT_ID=your_naver_client_id
# NAVER_CLIENT_SECRET=your_naver_client_secret

# Apple
# APPLE_CLIENT_ID=your_apple_client_id
# APPLE_TEAM_ID=your_apple_team_id
# APPLE_KEY_ID=your_apple_key_id
